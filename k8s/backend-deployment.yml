kind: Deployment
apiVersion: apps/v1
metadata: 
  name: backend-deployment
  namespace: codegalaxy-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: code_back
  template:
    metadata:
      labels: 
        app: code_back
    spec:
      containers:
      - name: code-backend
        image: gehlotnikhil38/codegalaxy-server:latest
        ports:
        - containerPort: 8000
        resources: 
          requests: 
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi